import { SearchByIdOrNamePipe } from './search-by-id-or-name.pipe';

describe('SearchByIdOrNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByIdOrNamePipe();
    expect(pipe).toBeTruthy();
  });
});
